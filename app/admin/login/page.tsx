'use client';import{useState}from'react';
export default function Login(){const[p,setP]=useState(''),[e,setE]=useState('');async function go(){const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:p})});if(r.ok)location.href='/admin';else setE('Incorrect password')}return <main className="login"><div className="brand">SOLOSALON</div><div className="card" style={{marginTop:30}}><h2>Stylist login</h2><p className="muted">Manage your bookings and availability.</p>{e&&<div className="notice">{e}</div>}<input type="password" placeholder="Admin password" value={p} onChange={x=>setP(x.target.value)}/><button style={{width:'100%',marginTop:10}} onClick={go}>Continue</button></div></main>}