'use client';import{useEffect,useState}from'react';const days=['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];export default function Availability(){const[h,setH]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/availability').then(r=>r.json()).then(j=>setH(j.hours||[]))},[]);async function save(){await fetch('/api/admin/availability',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({hours:h})});alert('Saved')}return <div className="card"><h3>Regular booking hours</h3><p className="muted">Anything outside these times cannot be booked online.</p>{h.map((x,i)=><div className="dayrow" key={x.weekday}><label className="toggleline daylabel"><input type="checkbox" checked={x.enabled} onChange={e=>{const n=[...h];n[i]={...x,enabled:e.target.checked};setH(n)}}/><b>{days[x.weekday]}</b></label><input type="time" disabled={!x.enabled} value={(x.start_time||'09:00').slice(0,5)} onChange={e=>{const n=[...h];n[i]={...x,start_time:e.target.value};setH(n)}}/><input type="time" disabled={!x.enabled} value={(x.end_time||'17:00').slice(0,5)} onChange={e=>{const n=[...h];n[i]={...x,end_time:e.target.value};setH(n)}}/></div>)}<button style={{marginTop:12}} onClick={save}>Save availability</button></div>}