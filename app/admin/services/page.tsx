'use client';import{useEffect,useState}from'react';
const blank={category:'Hair',name:'',description:'',duration_minutes:60,price_cents:0,buffer_before_minutes:0,buffer_after_minutes:0,active:true,sort_order:10};
export default function Services(){const[s,setS]=useState<any[]>([]),[f,setF]=useState<any>(blank),[id,setId]=useState<string|null>(null);async function load(){const j=await fetch('/api/admin/services').then(r=>r.json());setS(j.services||[])}useEffect(()=>{load()},[]);async function save(){await fetch('/api/admin/services',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...f,id:id||undefined})});setF(blank);setId(null);load()}function edit(x:any){setId(x.id);setF(x)}async function toggle(x:any){await fetch('/api/admin/services',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...x,active:!x.active})});load()}return <><div className="card"><h3>{id?'Edit service':'Add service'}</h3><div className="grid two"><input placeholder="Category" value={f.category||''} onChange={e=>setF({...f,category:e.target.value})}/><input placeholder="Service name" value={f.name||''} onChange={e=>setF({...f,name:e.target.value})}/><input type="number" placeholder="Price $" value={(f.price_cents||0)/100} onChange={e=>setF({...f,price_cents:Math.round(+e.target.value*100)})}/><input type="number" placeholder="Duration minutes" value={f.duration_minutes||60} onChange={e=>setF({...f,duration_minutes:+e.target.value})}/><input type="number" placeholder="Buffer before" value={f.buffer_before_minutes||0} onChange={e=>setF({...f,buffer_before_minutes:+e.target.value})}/><input type="number" placeholder="Buffer after" value={f.buffer_after_minutes||0} onChange={e=>setF({...f,buffer_after_minutes:+e.target.value})}/></div><textarea style={{marginTop:10}} placeholder="Description" value={f.description||''} onChange={e=>setF({...f,description:e.target.value})}/><label className="toggleline" style={{marginTop:10}}><input type="checkbox" checked={f.active} onChange={e=>setF({...f,active:e.target.checked})}/> Available for online booking</label><button style={{marginTop:10}} disabled={!f.name} onClick={save}>{id?'Save changes':'Add service'}</button></div><div className="card" style={{marginTop:14}}>{s.map(x=><div className="service-row" key={x.id}><div><b>{x.name}</b><div className="muted">{x.duration_minutes} min · {x.price_cents===0?'Free':'$'+(x.price_cents/100).toFixed(0)}</div></div><div><button className="outline smallbtn" onClick={()=>edit(x)}>Edit</button> <button className="ghost smallbtn" onClick={()=>toggle(x)}>{x.active?'Online ✓':'Hidden'}</button></div></div>)}</div></>}