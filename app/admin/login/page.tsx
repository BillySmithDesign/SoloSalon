'use client';import{useState}from'react';export default function Login(){let[p,setP]=useState(''),[e,setE]=useState('');async function go(){let r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:p})});if(r.ok)location.href='/admin/calendar';else setE('Incorrect password')}return <main className="login"><div className="brand">OPENBOOK · SALON</div><div className="card" style={{marginTop:30}}><h2>Salon login</h2><p className="muted">Manage appointments, services and availability.</p>{e&&<div className="notice">{e}</div>}<input type="password" placeholder="Admin password" value={p} onChange={x=>setP(x.target.value)}/><button style={{width:'100%',marginTop:10}} onClick={go}>Continue</button></div></main>}