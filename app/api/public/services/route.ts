import{NextResponse}from'next/server';import{db}from'../../../../lib/db';import{SALON_ID}from'../../../../lib/config';export const dynamic='force-dynamic';
export async function GET(){try{const c=db();const[{data:services,error},{data:salon,error:se}]=await Promise.all([c.from('services').select('*').eq('salon_id',SALON_ID).eq('active',true).order('sort_order').order('created_at'),c.from('salons').select('name,stylist_name,location_text,booking_message,profile_image_url,instagram_url,facebook_url,tiktok_url,timezone').eq('id',SALON_ID).single()]);if(error)throw error;if(se)throw se;return NextResponse.json({services,salon})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}