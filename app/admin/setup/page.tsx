'use client';import{useState}from'react';export default function Setup(){const[done,setDone]=useState(false);async function finish(){await fetch('/api/admin/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({onboarding_complete:true})});location.href='/admin/settings'}return <main className="wizard"><div className="brand">SOLOSALON · SETUP</div><div className="card" style={{marginTop:20}}><h2>Welcome to SoloSalon</h2><p className="muted">Set your profile, services and availability from the admin dashboard. Your booking page will update automatically.</p><button onClick={finish}>Start setup</button></div></main>}