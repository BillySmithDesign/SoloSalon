import{NextRequest,NextResponse}from'next/server';import{requireAdmin}from'../../../../lib/auth';import{db}from'../../../../lib/db';import{SALON_ID}from'../../../../lib/config';
export async function POST(r:NextRequest){try{await requireAdmin();const b=await r.json(),u:any={};['booking_window_days','slot_interval_minutes','client_confirmation_enabled','salon_notification_enabled','onboarding_complete'].forEach(k=>{if(k in b)u[k]=b[k]});const{data,error}=await db().from('salons').update(u).eq('id',SALON_ID).select().single();if(error)throw error;return NextResponse.json({salon:data})}catch(e:any){return NextResponse.json({error:e.message},{status:e.message==='UNAUTHORISED'?401:500})}}