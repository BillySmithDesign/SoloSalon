import{db}from'../../../lib/db';import{SALON_ID}from'../../../lib/config';export const dynamic='force-dynamic';export default async function Page(){const{data}=await db().from('clients').select('*').eq('salon_id',SALON_ID).order('name');return <div className="card">{(data||[]).map((c:any)=><div className="service-row" key={c.id}><div><b>{c.name}</b><div className="muted">{c.email} · {c.phone}</div></div></div>)}</div>}